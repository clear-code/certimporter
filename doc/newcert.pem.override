# Format:
#   <domain>:<port>:<security exceptions>
# Available exception types:
#   1 = Ignore self-signed certs and unknown CA errors.
#   2 = Ignore self-signed, unknown CA, and "different host" errors.
#   4 = Ignore self-signed, unknown CA, and "expired" errors.
#   6 = Ignore self-signed, unknown CA, "different host", and "expired" errors.
site.example.com:443:1
foo.example.com:443:2
bar.example.com:443:6
